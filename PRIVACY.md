# ReleaseSmith 隐私说明

ReleaseSmith（发版匠）采用本地优先设计。

## 本地项目扫描

- 项目类型识别、配置解析、发布体检、产物扫描和 SHA-256 均在本机完成；
- 扫描和检查阶段不会上传项目源代码；只有用户明确选择源码发布并确认后才会推送；
- 默认扫描只读，不会修改项目配置、版本号、Git 提交、Git 标签或工作区文件；
- 扫描具有深度和文件数量上限，并忽略 `.git`、`node_modules`、调试、缓存和临时目录；
- ReleaseSmith 不读取证书私钥、证书密码或其他无关目录。

## 本地数据

- 最近项目摘要、应用设置和发布会话均以带 schema 版本的 JSON 文件保存在系统应用数据目录；
- 旧版 WebView 最近项目摘要只在升级时读取一次，迁移成功后删除；
- 本地数据采用临时文件和备份恢复机制保存；单个损坏会话不会影响其他发布历史；
- 发布会话不会默认写入被扫描的项目；
- 发布会话只记录发布准备所需的项目摘要、本地路径、编辑内容和产物元数据；
- 删除发布会话只删除 ReleaseSmith 的本地记录，不删除项目或远程 Release。

## Git 与 GitHub

- Git 命令由 Rust 使用固定参数数组执行，不通过 shell 拼接用户输入；
- ReleaseSmith 不会自动执行 `commit`、`checkout`、`reset` 或 `rebase`；源码发布经确认后会将已经提交的内容推送到分支，并将当前提交推送为指定标签；
- GitHub 写操作通过用户本机的 GitHub CLI 执行；
- ReleaseSmith 不读取 GitHub CLI 保存 Token 的文件，也不会保存或记录 Token、Cookie 或密码；
- 只有用户查看确认页、通过发布前检查并明确勾选确认后，才允许创建 GitHub Draft Release，或创建/调整源码仓库并推送已提交内容；
- Rust 后端强制 Draft 状态，不能通过修改前端数据创建正式 Release；
- 已存在的远程标签会在发布前阻止操作，不会删除或覆盖远程标签与 Release。

GitHub CLI 会根据用户自己的认证状态与 GitHub 通信。仓库信息、Release 正文和用户选择的安装包只会在用户确认创建 Draft Release 后发送给 GitHub；源代码和指定标签只会在用户明确选择源码模式并确认后推送。源码模式会执行 `gh auth setup-git`，使 Git 推送使用同一份本机认证。应用更新检查只读取配置的公开 `latest.json`；只有用户确认安装更新后才下载签名安装包并重启。

## 文案、二维码与海报

宣传文案模板、二维码和海报均在本机生成，不调用第三方二维码或在线截图服务。产品截图只有在用户通过文件选择器明确选择后才会读取，仅用于本地海报合成。

ReleaseSmith 不会自动登录或发布到微信、小红书等社交平台，也不会通过浏览器自动化模拟用户发布。

## 本地资料包导出

- 只有用户选择目标目录并点击导出后才写入文件；
- 资料包先写入临时目录，成功后再整体移动到最终目录；失败或取消会清理临时内容；
- 同名目录不会被覆盖，而是创建带时间后缀的新目录；
- 安装包默认不复制，只有用户勾选后才复制进资料包；
- 导出不会上传任何内容。

## 数据采集

当前版本不包含遥测、广告或项目代码采集功能。ReleaseSmith 不会将发布资料上传到自有服务器。
