# 更新日志

本文件记录 ReleaseSmith（发版匠）的重要版本变化。

## 0.3.0 - 2026-08-05

### 新增

- 新增“自动跨平台发布”模式：一次确认即可准备私有源码仓、公开发布仓并推送触发 GitHub Actions 的版本标签；
- 宣传文案和海报步骤改为可选，不再阻塞源码或安装包发布；
- 源码发布模式同步推送分支和发布标签，可触发源码仓库中的 GitHub Actions；
- Tauri updater 签名更新检查与设置页安装入口；
- 私有源码仓与公开发布仓分离的跨平台 GitHub Actions 模板。
- 启动时先选择项目，已完成的发布流程不再自动占据发布向导。
- 检测到未提交修改或自上次发布后的新提交时，提醒使用自动跨平台发布。

## 0.2.0 - 2026-07-29

### 新增

- 九步发布向导，串联项目加载、发布体检、发布信息、更新日志、发布产物、GitHub 草稿、宣传文案、海报和资料包导出；
- 可恢复、可复制的本地发布会话和发布历史；
- Windows、macOS、Linux 常见发布产物识别与流式 SHA-256；
- 基于 Git 提交的可编辑更新日志；
- 通过 GitHub CLI 创建强制 Draft Release，并在创建前执行权限、标签、文件和远程副作用检查；
- 朋友圈、小红书文案、下载二维码及三种尺寸的本地宣传海报；
- 完整发布资料包的原子化导出与任务取消；
- 最近项目、启动恢复、默认导出目录和数据目录设置。

### 改进

- 重新设计 macOS 应用图标与深色桌面界面；
- 大型项目扫描时优先保留关键配置、文档和图标；
- 旧版发布会话和应用设置支持 schema 迁移、备份恢复与损坏文件隔离；
- 上游内容发生变化时自动使旧 GitHub 草稿和导出结果失效；
- 默认窗口宽度下的发布流程顶栏可自适应收缩。

### 安全

- 项目扫描默认只读，源代码和发布资料不上传；
- Git 与 GitHub CLI 使用固定程序和参数数组，不执行前端传入的命令字符串；
- GitHub Release 在前端和 Rust 后端均强制为 Draft；
- 剪贴板只开放文本写入，外部链接限制为明确允许的 HTTPS 地址；
- 日志不记录 Token、密码、证书私钥或用户文案正文。

## 0.1.0 - 2026-07-25

### 新增

- Tauri、Electron、Node.js、Rust 和通用 Git 项目识别；
- 项目名称、版本、Git 状态、图标、文档和发布产物体检；
- 发布准备度评分、检查清单和本地宣传资料生成基础能力。
